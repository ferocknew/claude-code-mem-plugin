{
  "enabled": true,
  "max_entities": 5,
  "injection_mode": "auto",
  "show_marker": true,
  "debug": true,
  "use_llm_keywords": true,
  "llm_keywords_timeout": 3000,
  "project_isolation": true,
  "include_other_projects": false,
  "worker_port": 57777,
  "worker_host": "127.0.0.1",
  "mem_jsonl": "",
  "min_score_threshold": 30,
  "description": {
    "enabled": "是否启用记忆注入",
    "max_entities": "最多注入几个实体",
    "injection_mode": "注入模式: auto(自动), always(总是), manual(手动)",
    "show_marker": "是否显示 🧠 标记(方便验证插件是否工作)",
    "debug": "是否显示调试信息",
    "use_llm_keywords": "是否使用 LLM 智能提取关键词(需要 API Key)",
    "llm_keywords_timeout": "LLM 关键词提取超时时间(毫秒)，超时后回退到简单提取",
    "project_isolation": "是否只注入当前项目的记忆",
    "include_other_projects": "是否包含其他项目的记忆(当 project_isolation=true 时)",
    "worker_port": "Worker 服务端口",
    "worker_host": "Worker 服务主机地址",
    "min_score_threshold": "最低评分阈值，低于此分数的记忆不会被注入(默认30分)"
  }
}
