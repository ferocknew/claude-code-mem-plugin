# 只包含 plugin 目录及必要文件
graft plugin

# 包含必要的顶层文件
include LICENSE
include README.md
include VERSION

# 排除开发文件
global-exclude .git*
global-exclude .DS_Store
global-exclude __pycache__
global-exclude *.py[cod]
global-exclude *.so
